# Placeholders

Punch-list of every placeholder value used on the Viscraft site that the business owner must supply real data for, per the "never invent" rule in `01-overview.md`. Whenever a page is built that touches one of these fields, use an obvious placeholder token (e.g. `[[CIN — TO SUPPLY]]`) and add it here if not already listed.

- Legal entity name — placeholder (confirmed 2026-09-14)
- CIN — placeholder
- GSTIN — placeholder
- Registered office address — placeholder
- Grievance officer name/contact — placeholder
- Business phone number — placeholder
- WhatsApp number — placeholder (confirmed 2026-09-14)
- Testimonials — not yet created; must be clearly-labelled placeholders when they appear, never invented
- Trip case studies — not yet created; same rule
- Success/approval rate figures — must never appear at all, not even as placeholders (binding constraint, see `02-copy-constraints.md`)
- Any credential/accreditation/affiliation/partnership claim — must never appear at all, same as above
